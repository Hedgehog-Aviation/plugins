# plugins
A file containing (almost) all available VatSys plugins on GitHub. Why? Basically every plugin tool for vatSys uses a file like this to pull plugin URLs, and being such a nosy person I always fork them, so this one URL will serve for all my tools.

If you too would like your plugin added, just make a PR!
